import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    backgroundColor: '#1f5998',
    borderRadius: 7,
    color: 'white',
    width: '90%',
    padding:6,
    textAlign:'center'
  },
});

export default styles;